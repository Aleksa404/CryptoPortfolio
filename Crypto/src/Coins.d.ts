export interface coinSearch {
  id: number;
  coinName: string;
  price: number;
  symbol: string;
  marketCap: number;
}
export interface coinProfile {
  coinName: string;
  price: number;
  symbol: string;
  marketCap: number;
}
