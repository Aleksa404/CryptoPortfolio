export interface coinSearch {
  coinName: string;
  price: number;
  symbol: string;
  marketCap: number;
}
export interface coinProfile {
  coinName: string;
  price: number;
  symbol: string;
  marketCap: number;
}
